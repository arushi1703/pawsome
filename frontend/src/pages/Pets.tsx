import React from 'react'
import useGetPets from '../hooks/pets/useGetPets';

const Pets = () => {
    //const { loading, pets } = useGetPets();
  return (
    <div>Pets</div>
  )
}

export default Pets

